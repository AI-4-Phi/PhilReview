# Test dependencies for philosophy-research skill scripts
#
# Install with: pip install -r tests/requirements-test.txt
#
# These are in addition to the main skill dependencies:
#   requests, beautifulsoup4, lxml, pyalex, arxiv

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# Mocking (included in Python 3.3+, but explicit for clarity)
# unittest.mock is used, no extra package needed

# For async tests if needed in future
pytest-asyncio>=0.21.0

# For test timing and profiling
pytest-timeout>=2.0.0

# Coverage reporting
coverage>=7.0.0
